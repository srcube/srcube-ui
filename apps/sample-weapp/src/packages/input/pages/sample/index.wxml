<app-page
 title="Input" description="用于收集短文本输入，支持清晰的状态和校验反馈。适合表单、搜索和结构化文本录入。" tone="{{tone}}" withBack="{{true}}" contentClassName="space-y-6">
    <view class="rounded-2xl p-4 shadow-sm {{tone === 'dark' ? 'border border-zinc-800 bg-black text-zinc-50 shadow-black/30' : 'bg-white text-slate-900'}}">
      <view class="text-sm font-semibold">Basic</view>
      <view class="mt-1 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">Input 复用 Field 的容器能力</view>

      <sr-input
        class="mt-3"
        label="手机号"
        value="{{basicValue}}"
        placeholder="请输入手机号"
        description="支持中国大陆手机号"
        isClearable
        hasEndContent
        bind:valuechange="handleBasicValueChange"
       tone="{{tone}}">
        <view slot="end" class="flex items-center justify-center">
          <view class="icon-[mingcute--phone-fill]" />
        </view>
      </sr-input>

      <view class="mt-2 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">Value: {{basicValue || 'empty'}}</view>
    </view>

    <view class="rounded-2xl p-4 shadow-sm {{tone === 'dark' ? 'border border-zinc-800 bg-black text-zinc-50 shadow-black/30' : 'bg-white text-slate-900'}}">
      <view class="text-sm font-semibold">Label Placement</view>
      <view class="mt-1 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">outside / outside-left / inside</view>

      <view class="mt-3 space-y-3">
        <view>
          <sr-input
            label="Outside"
            value="field value"
            labelPlacement="outside"
            isClearable
           tone="{{tone}}" />
        </view>
        <view>
          <sr-input
            label="Outside Left"
            value="field value"
            labelPlacement="outside-left"
            isClearable
           tone="{{tone}}" />
        </view>
        <view>
          <sr-input
            label="Inside"
            value="field value"
            labelPlacement="inside"
            isClearable
           tone="{{tone}}" />
        </view>
      </view>
    </view>

    <view class="rounded-2xl p-4 shadow-sm {{tone === 'dark' ? 'border border-zinc-800 bg-black text-zinc-50 shadow-black/30' : 'bg-white text-slate-900'}}">
      <view class="text-sm font-semibold">Variants</view>
      <view class="mt-1 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">default / outline / twotone / underline</view>

      <view class="mt-3 space-y-3">
        <view>
          <sr-input label="Default" value="field value" variant="default"  tone="{{tone}}" />
        </view>
        <view>
          <sr-input label="Outline" value="field value" variant="outline"  tone="{{tone}}" />
        </view>
        <view>
          <sr-input label="Twotone" value="field value" variant="twotone"  tone="{{tone}}" />
        </view>
        <view>
          <sr-input label="Underline" value="field value" variant="underline"  tone="{{tone}}" />
        </view>
      </view>
    </view>

    <view class="rounded-2xl p-4 shadow-sm {{tone === 'dark' ? 'border border-zinc-800 bg-black text-zinc-50 shadow-black/30' : 'bg-white text-slate-900'}}">
      <view class="text-sm font-semibold">Sizes</view>
      <view class="mt-1 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">sm / md / lg</view>

      <view class="mt-3 space-y-3">
        <view>
          <sr-input label="Small" value="field value" size="sm"  tone="{{tone}}" />
        </view>
        <view>
          <sr-input label="Medium" value="field value" size="md"  tone="{{tone}}" />
        </view>
        <view>
          <sr-input label="Large" value="field value" size="lg"  tone="{{tone}}" />
        </view>
      </view>
    </view>

    <view class="rounded-2xl p-4 shadow-sm {{tone === 'dark' ? 'border border-zinc-800 bg-black text-zinc-50 shadow-black/30' : 'bg-white text-slate-900'}}">
      <view class="text-sm font-semibold">States</view>
      <view class="mt-1 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">invalid / disabled / readOnly</view>

      <view class="mt-3 space-y-3">
        <view>
          <sr-input
            label="Invalid"
            value="invalid value"
            errorMessage="该字段格式不正确"
            variant="outline"
            color="danger"
            isInvalid
           tone="{{tone}}" />
        </view>
        <view>
          <sr-input
            label="Disabled"
            value="disabled value"
            description="disabled state"
            isDisabled
           tone="{{tone}}" />
        </view>
        <view>
          <sr-input
            label="Readonly"
            value="readonly value"
            description="readonly state"
            isReadOnly
           tone="{{tone}}" />
        </view>
      </view>
    </view>

    <view class="rounded-2xl p-4 shadow-sm {{tone === 'dark' ? 'border border-zinc-800 bg-black text-zinc-50 shadow-black/30' : 'bg-white text-slate-900'}}">
      <view class="text-sm font-semibold">Colors</view>
      <view class="mt-1 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">use 2 sm ButtonGroup to switch color</view>

      <view class="mt-3 flex flex-col gap-2">
        <block
          wx:for="{{colorGroups}}"
          wx:for-item="group"
          wx:for-index="groupIndex"
          wx:key="groupIndex"
        >
          <sr-button-group size="sm" isBlock>
            <block wx:for="{{group}}" wx:for-item="option" wx:key="value">
              <sr-button
                class="flex-1"
                color="{{activeColor === option.value ? option.value : 'default'}}"
                variant="{{activeColor === option.value ? 'solid' : 'flat'}}"
                data-color="{{option.value}}"
                bind:tap="handleColorTap"
              >
                {{option.label}}
              </sr-button>
            </block>
          </sr-button-group>
        </block>
      </view>

      <sr-input
        class="mt-3"
        label="Color Preview"
        value="{{colorValue}}"
        color="{{activeColor}}"
        isClearable
        bind:valuechange="handleColorValueChange"
       tone="{{tone}}" />

      <view class="mt-2 text-xs {{tone === 'dark' ? 'text-zinc-400' : 'text-slate-500'}}">Color: {{activeColor}}</view>
    </view>
</app-page>
