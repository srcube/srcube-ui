<sr-navbar
  className="sticky top-0 z-20 {{tone === 'dark' ? 'border-b border-zinc-800' : 'border-b border-slate-200'}}"
  title="{{title}}"
  tone="{{tone}}"
  withBack="{{withBack}}"
  hasSafeTop="{{true}}"
/>
