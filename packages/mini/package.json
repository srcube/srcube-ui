{
  "name": "@srcube-ui/mini",
  "private": true,
  "type": "module",
  "files": ["dist"],
  "miniprogram": "dist",
  "scripts": {
    "dev": "tsdown --watch --config tsdown.config.ts",
    "build": "tsdown --config tsdown.config.ts",
    "test": "pnpm -w vitest --run --project unit packages/mini/__tests__"
  },
  "dependencies": {
    "@srcube-ui/styles": "workspace:*",
    "miniprogram-computed": "catalog:"
  },
  "devDependencies": {
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
